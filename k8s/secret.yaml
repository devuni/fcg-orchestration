apiVersion: v1
kind: Secret
metadata:
  name: fcg-fase2-secrets
type: Opaque
stringData:
  # RabbitMQ (sensível)
  RABBITMQ_USERNAME: "guest"
  RABBITMQ_PASSWORD: "guest"

  # JWT (sensível) - troque para um valor forte antes de apresentar
  JWT_SECRET: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+"
  JWT_ISSUER: "fcg-fase2"
  JWT_AUDIENCE: "fcg-fase2"

  # Connection Strings SQLite (o professor pediu em Secret)
  USERS_DB: "Data Source=/data/users.db"
  CATALOG_DB: "Data Source=/data/catalog.db"
